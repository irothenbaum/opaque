const TC = require('../../../../TwoCylinder/dist/twocylinder')


// TODO: What properties does every enemy have? color, 3 health bars, a dying process... what else?
class Enemy extends TC.Engine.Entity {
    constructor(options) {
        super(options)
    }
}